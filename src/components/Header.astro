---
export interface Props {
	isHome?: boolean;
}

const { isHome } = Astro.props;
---

<header>
	<div class={`flex justify-between items-center ${ isHome ? 'mt-20 lg:mt-40' : 'mt-10 lg:mt-20' }`}>
		<h2 class="text-lg sm:text-xl lg:mb-10 md:text-2xl lg:text-3xl text-gray-900 font-medium md:font-normal leading-none">
			<a href="/">{isHome ? "Jordi Villar" : "▴"}</a>
		</h2>
	</div>
</header>
